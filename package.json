{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "^3.20.2",
    "prettier": "^2.0.0",
    "wrangler": "^0.0.2"
  },
  "scripts": {
    "docs:dev": "yarn --cwd ./docs dev",
    "docs:build": "yarn --cwd ./docs build",
    "wrangler:publish": "wrangler publish && rm -rf dist docs/worker"
  },
  "workspaces": [
    "api",
    "docs"
  ]
}
